# Google AI API Key - Bu anahtarı Render environment variables'ına ekleyin
GOOGLE_API_KEY=your_google_ai_api_key_here

# Embedding Model (varsayılan)
EMBEDDING_MODEL=models/embedding-001

# Chunk ayarları (varsayılan değerler)
CHUNK_SIZE=1000
CHUNK_OVERLAP=200

# Vector DB Path - Render persistent disk için özel ayar
# Local development için: ./chroma_db
# Render production için: /var/data/chroma_db
VECTOR_DB_PATH=/var/data/chroma_db

# Port - Render otomatik set eder
PORT=10000
